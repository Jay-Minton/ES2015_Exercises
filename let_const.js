//1
const PI = 3.14;

//2
/*var is globally scoped and the same variable can be declared multiple times.
  let is block scoped and a varible can only be declared once per block.
  both can be reasigned once they've been declared.*/

//3
/*var is globally scoped and the same variable can be declared multiple times.
  const is block scoped and a varible can only be declared once per block.
  and cannot be reasigned once it's been declared.*/

//4
/** let and const are both block scoped and can only be declared one per block
 * but let can be reasigned while const cannont.*/

 //5
 /*Variables are "hoisted" to the top of the scope in which they exist which means
 var style variables can be accessed before they are even declared
  */